# Exports em bruto das bases de dados

Coloca aqui os ficheiros exportados (CSV ou BibTeX), um por base:

- `scopus.csv`
- `ieee.csv`
- `acm.csv`

NAO editar estes ficheiros a mao — sao o registo do que a base devolveu.
A triagem faz-se em `../screening.csv`, gerado por `scripts/slr_pipeline.py ingest`.
